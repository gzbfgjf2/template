```bash
git clone -o template https://github.com/gzbfgjf2/template.git my-project && cd my-project
git switch -c main
# develop your repo
git add remote origin <my-repo-remote-url>
git push -u origin main
```
